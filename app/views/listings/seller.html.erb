<table class="table table-striped table-bordered">
  <tr>  
    <th class="center">Image</th>
    <th class="center">Name</th>
    <th class="center">Description</th>
    <th class="center">Price</th>
    <th class="center">Actions</th>
  </tr>

  <% @listings.each do |listing| %>
  <tr>



    <td><%= image_tag listing.image.url (:thumb)%></td>
    <td><%= listing.name %></td>
    <td><%= listing.description %></td>
    <td><%= number_to_currency(listing.price) %></td>
    <td>
      <div class="dropdown">
        <button class="btn btn btn-primary dropdown-toggle" type="button" id="dropdownMenu1" data-toggle="dropdown">
          Action
          <span class="caret"></span>
        </button>
        <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu1">
          <li role="presentation"><a role="menuitem" tabindex="-1" <%= link_to "View", listing, class: "btn btn-link"  %> </a></li>
          <li role="presentation"><a role="menuitem" tabindex="-1" <%= link_to "Edit", edit_listing_path(listing), class: "btn btn-link"  %> </a></li>
          <li role="presentation"><a role="menuitem" tabindex="-1" <%= link_to 'Destroy', listing, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-link" %> </a></li>

        </ul>
      </div>
      
    </td>
  </tr>
  <%end%>
</table>




<!--

Before table Myself added
<div class="center">
  <div class="row">
    <% @listings.each do |listing| %>
    <div class="col-md-3">
      <div class="thumbnail">
        <%= link_to image_tag(listing.image.url), listing %>
        <div class="caption">
          <h3><%= listing.name %></h3> 
          <p> <%= number_to_currency(listing.price) %></p>
          <p> <%= "sold by #{listing.user.name}" %> </p>
          <% if (user_signed_in?) && (current_user == listing.user) %>
          <%= link_to 'Edit', edit_listing_path(listing), class: "btn btn-link" %>
          <%= link_to 'Destroy', listing, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-link" %> 
          <% end %>

        </div>
      </div>

    </div>
    <% end %>
  </div>

</div>
-->
<% if (user_signed_in?) %>
<%= link_to 'New Listing', new_listing_path, class: "btn btn-link" %>
<% end %>